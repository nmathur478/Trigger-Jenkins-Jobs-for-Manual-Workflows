####
name: Trigger Jenkins Job

on: 
  push:
    branches:
      - "main"

jobs:
  trigger-jenkins-job:
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - name: Trigger Jobs
        run: |
          ls -al
  
          
